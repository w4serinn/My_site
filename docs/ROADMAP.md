# ROADMAP

## コンセプト

- 種別：自己紹介＋作品ギャラリーの「全部乗せ」個人サイト（waserin）
- **完成の定義**：トップページ（自己紹介＋今ある作品のカード一覧）＋必要な
  個別ページが揃った時点で「枠として完成」。
- 配色・デザインテイストは2026-08-03にユーザーが確定：ダーク基調＋ネオン寄りの
  アクセントカラー（ゲーム/学院・パズル系の作品と親和性のある方向性）。
  `styles/tokens.css`に反映済み。
- **2026-08-03追記（ユーザー指示）**：確定タスクが尽きて着手できるものが
  content-pending/blockedのみになった場合、evolveループは承認を待たずに
  新規ページ・新規コンテンツの枠を自律的に考案・実装してよい
  （実際の本文はこれまで通りcontent-pendingとし、無断創作しない）。
  以前記載していた「新規ページは確定タスク完了後の別枠」という着手ゲートは、
  この指示により撤廃済み。

<!-- 「枠として完成」した場合、ここに1行追記する -->

## バグ修正（最優先）

（現時点でなし）

## 確定タスク

### 1. トップページ：自己紹介
status: content-pending / サイズ: M

- [x] 自己紹介ブロックの構成を決める（名前・肩書き/一言・興味の方向性の3項目。
      SNSリンク欄は「3. 連絡先・SNSリンク」のcontactセクションと重複するため
      自己紹介ブロックには含めない方針とした）
- [x] 名前は「waserin」で確定。それ以外の本文は `[要入力：〜について]` を配置
- [ ] `[要入力：肩書き・一言]` `[要入力：興味の方向性]` の本文入力待ち
      （入力され次第、次サイクルで `完了` に変更する）

### 2. トップページ：作品一覧カード
status: content-pending / サイズ: M

- [x] カードのフォーマットを決める（サムネ枠・タイトル・一言説明・リンクボタンの4要素、
      `.work-card`としてグリッド表示）
- [x] カード1：アルノルド（王立魔法学院アルノルド）
      - タイトル「王立魔法学院アルノルド」・リンク先`https://arnold-academy.com`を確定
      - 一言説明は `[要入力：アルノルドの紹介文]`（本文入力待ち）
- [x] カード2：マイン数独
      - タイトル欄「マイン数独」は確定、リンク先URLは `[要入力：マイン数独の公開URL]`
      - 一言説明は `[要入力：マイン数独の紹介文]`
- [ ] 一言説明2件・マイン数独のURLの本文入力待ち
      （入力され次第、次サイクルで `完了` に変更する）

### 3. 連絡先・SNSリンク
status: content-pending / サイズ: S

- [x] 連絡先セクションの枠を作る（`.contact-list`のアイコン＋リンクのリスト形式。
      リンク数に応じて`.contact-item`を複製できる構造）
- [ ] 具体的なリンク先は `[要入力：SNS/連絡先リンク]`（本文入力待ち。
      複数ある場合は`.contact-item`を追加する）

### 4. 仕上げ
status: content-pending / サイズ: S

- [x] レスポンシブ対応（480px以下でヘッダー/フッター/mainの余白を縮小、
      作品カードを1カラムに）
- [x] favicon の設置（`assets/favicon.svg`、暫定プレースホルダー）
- [x] OGP設定（`assets/og-image.svg`を暫定プレースホルダーとして設置。
      タイトルは「waserin」で確定、説明文は `[要入力：サイトの説明文]`）
- [x] `og:image`のURLを絶対パス化（`https://w4serinn.github.io/My_site/assets/og-image.svg`。
      GitHub Pagesのデフォルト配信先を前提とした値。カスタムドメインを設定する
      場合は要更新）
- [ ] 説明文（description/og:description）の本文入力待ち

### 5. Nowページ（今何してるかの近況ページ）
status: content-pending / サイズ: S

<!-- 2026-08-03にユーザーが直接指示し、確定タスク1〜4のcontent-pending解消を
     待たずに着手。新規ページ提案候補から「Now page」を選定して確定タスクへ
     格上げした（下記候補リストからは退避） -->

- [x] `now.html`を新規作成し、ヘッダー/フッターは`<!-- HEADER -->`/`<!-- FOOTER -->`
      プレースホルダーを使用
- [x] `partials/header.html`のナビに「Now」リンクを追加
- [x] 本文は `[要入力：今何をしているか]` のプレースホルダーを配置
- [x] `now.html`にindex.htmlと同一方針のOGPメタタグを追加（og:title「Now | waserin」、
      og:imageは確定済みの絶対URLを流用）
- [ ] `[要入力：今何をしているか]` の本文入力待ち
      （入力され次第、次サイクルで `完了` に変更する）

### 6. Usesページ（使っているツール・環境紹介）
status: content-pending / サイズ: S

<!-- 2026-08-03にユーザー指示で新規ページ提案候補から確定タスクへ格上げ -->

- [x] `uses.html`を新規作成し、リスト形式（`.list-page`）の枠を作成
- [x] `partials/header.html`のナビに「Uses」リンクを追加
- [x] 項目は `[要入力：使用ツール・環境]` のプレースホルダーを配置
- [ ] 具体的なツール名の本文入力待ち（入力され次第 `完了` に変更する）

### 7. Favoritesページ（好きなものリスト）
status: content-pending / サイズ: S

<!-- 2026-08-03にユーザー指示で新規ページ提案候補から確定タスクへ格上げ -->

- [x] `favorites.html`を新規作成し、リスト形式（`.list-page`）の枠を作成
- [x] `partials/header.html`のナビに「Favorites」リンクを追加
- [x] 項目は `[要入力：好きなもの]` のプレースホルダーを配置
- [ ] 具体的な項目の本文入力待ち（入力され次第 `完了` に変更する）

### 8. Timelineページ（年表・タイムライン）
status: content-pending / サイズ: S

<!-- 2026-08-03にユーザー指示で新規ページ提案候補から確定タスクへ格上げ -->

- [x] `timeline.html`を新規作成し、年＋出来事のリスト形式（`.timeline`）の枠を作成
- [x] `partials/header.html`のナビに「Timeline」リンクを追加
- [x] 項目は `[要入力：年]` / `[要入力：出来事]` のプレースホルダーを配置
- [ ] 具体的な年表の本文入力待ち（入力され次第 `完了` に変更する）

### 9. 隠しページ・イースターエッグ
status: content-pending / サイズ: S

<!-- 2026-08-03にユーザー指示で新規ページ提案候補から確定タスクへ格上げ。
     「隠し」の性質上、ヘッダーの主要ナビには載せず、フッターに控えめな
     トリガーリンクを設置する形とした -->

- [x] `secret.html`を新規作成
- [x] `partials/footer.html`にごく控えめなトリガーリンク（コピーライト表記末尾の
      小さなリンク）を設置。主要ナビには載せない
- [x] 本文は `[要入力：隠しページの内容]` のプレースホルダーを配置
- [ ] 実際のイースターエッグ内容の本文入力待ち（入力され次第 `完了` に変更する）

### 10. Colophonページ（このサイトについて）
status: 完了 / サイズ: S

<!-- 2026-08-03にユーザーの「他も思いつく限りやっていい」指示を受けてループが
     新規考案。サイト自体の構築情報（技術スタック・ホスティング等）は
     客観的事実であり本文を無断創作したことにはならないため、content-pending
     とせず即completeとした -->

- [x] `colophon.html`を新規作成
- [x] 本文はサイトの構築事実（Claude Codeの`/evolve`自動サイクルで構築、
      素のHTML/CSS/JS、GitHub Pagesでホスティング、ソースへのリンク）を記載
- [x] `partials/footer.html`にColophonへのリンクを設置

## 今後のタスク候補

（確定タスク・提案候補がすべて尽きた場合、evolveループが自律的に新規ページ・
コンテンツ案をここか確定タスクへ追記してよい。2026-08-03のユーザー指示を参照）
