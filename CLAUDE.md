# このリポジトリでの作業方針

## 返答は日本語で行う

ユーザーとのチャットでの返答は、常に日本語で行うこと。
コード中のコメント・commitメッセージ・ドキュメント類も日本語でよい。

## 手動でのやり取り（evolveサイクル外）では、明記されない限り実装しない

`/evolve` のcron自動起動サイクル以外で、ユーザーと直接チャットでやり取りしている間
（設計相談・感想・「〜だったらよかった」のような振り返りコメントなど）は、
**ユーザーが明示的に「実装して」「進めて」等と指示するまでコードの実装をしないこと**。

- 設計についての意見・気づき・改善案が出た場合は、まず `docs/ROADMAP.md`
  （該当セクション、または新規タスクとして）に書き留めるか、口頭で認識を
  合わせるだけに留める。ファイルの編集・新規作成はしない。
- 「タスクに入れて」と言われた場合、それは `docs/ROADMAP.md` へのタスク追記を指す
  （Claude Code内蔵のTaskCreate/TodoWriteツールのことではない）。
- 実装してよいか迷う場合は、実装せずに一言確認してから進める。
- この方針は `evolve` skill（cron起動の自動サイクル）には適用されない。
  `/evolve` が正規のサイクルとして起動された際は、通常どおり
  `.claude/skills/evolve/SKILL.md` の手順に従って実装・テスト・commit・pushを行う。

## サイトのコンセプト（確定）

- 種別：自己紹介＋作品ギャラリーの「全部乗せ」個人サイト
- 名前表記：waserin
- **完成の定義**：トップページ（自己紹介＋今ある作品のカード一覧）＋必要な
  個別ページが揃った時点で「枠として完成」とする。今後の新作追加や、
  「新規ページ提案候補」（下記）の実装は、完成後の別枠タスクとして扱う。
- 内側のジャンル分け・自動化方針・配色やデザインテイストは未確定であり、
  evolveループが完成を目指す過程で判断してよい（承認は不要、判断に迷った
  場合のみ `docs/ROADMAP.md` に書き留めて一言確認する）。

## content-pending（本文未入力）の扱い

自己紹介や作品説明の**本文（実際のテキスト）はevolveループが勝手に創作しない**。

- 見出し・項目構成・カードのフォーマットはループが決めてよい
- 本文が必要な箇所には `[要入力：〜について]` の形でプレースホルダーを置く
- そのタスクの status は `完了` にせず `content-pending` にする
  （`docs/ROADMAP.md` 内での扱いは `blocked` と同様、承認/入力待ちの状態）
- ユーザーがプレースホルダーを実テキストに差し替えたら、次のサイクルでそれを
  検知して `完了` に変えてよい
- commit前に、今回のdiffで `content-pending` 対象のプレースホルダーが
  意図せず本文っぽいテキストに置き換わっていないか軽く確認すること
  （`.claude/skills/evolve/SKILL.md` の4節を参照）
